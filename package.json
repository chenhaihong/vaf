{
  "name": "vaf-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "pnpm run -r --parallel clean",
    "dev": "pnpm -C play dev",
    "preview": "pnpm -C vaf build && pnpm -C play preview",
    "build": "pnpm -C vaf build",
    "pb": "pnpm -C vaf pb",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "cz-git": "^1.3.12",
    "typescript": "^4.9.3"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-git"
    }
  }
}
